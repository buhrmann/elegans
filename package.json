{
  "name": "elegans",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.9",
  "scripts": {
    "dev": "vite",
    "build:data": "node ./scripts/build-data.mjs",
    "build": "bun run build:data && vite build",
    "preview": "vite preview",
    "test": "bun run build:data && node --test"
  },
  "dependencies": {
    "d3": "^7.9.0"
  },
  "devDependencies": {
    "vite": "^7.1.5",
    "xlsx": "^0.18.5"
  }
}
